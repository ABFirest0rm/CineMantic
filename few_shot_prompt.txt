FEW_SHOT_PROMPT = """
Task: Help users find horror movies based on their description, mood, or fuzzy memory.
You will extract structured metadata from the input text.

Output format:
Return ONLY valid JSON with the following keys:
- overview: 1–2 sentences in the style of a TMDB overview. Rephrase input as a story premise.
  Always phrase it as a story, never as "a movie that has...". Keep it suspenseful and spoiler-free.
- genres: Only include non-horror genres if they clearly apply (e.g., Sci-Fi, Action, Fantasy, Romance, Mystery, Drama).
  If the description is purely horror-related, leave this as an empty list [].
- keywords: 5–10 concise descriptive tags. These may include:
  - entities (e.g., vampires, haunted house, teenagers)
  - settings (e.g., 1980s, small town, forest, isolated village)
  - themes (e.g., survival, revenge, possession)
  - moods/tones (e.g., atmospheric, surreal, dreamlike, bleak, eerie)
  Mood descriptors represent FEEL, not literal events.

Hard rules:
- NO extra text, NO markdown, NO explanations.
- Do NOT invent names, characters, or twists not present in the input.
- Do NOT output "Setting", "Characters", "Plot Points", or anything outside JSON.
- If the input is vague, keep the output vague but valid.
- Output must be parseable JSON only.

---

### Example 1

Input:
"A surreal, atmospheric horror about a remote Scottish village."

Output:
{
  "overview": "A surreal and atmospheric horror story unfolding in a remote Scottish village.",
  "genres": ["Mystery"],
  "keywords": ["surreal", "atmospheric", "Scottish village", "remote setting", "isolation", "eerie tone"]
}

---

### Example 2

Input:
"A group of teenagers battle vampires in a small 1980s American town."

Output:
{
  "overview": "In a 1980s American town, a group of teenagers fight against a vampire threat.",
  "genres": [],
  "keywords": ["teenagers", "vampires", "1980s", "small town", "battle", "survival"]
}
"""
